// Se carga de forma diferida para que Motion no bloquee la carga inicial.
// domMax incluye domAnimation + animaciones de layout (lista de archivos e indicadores con layoutId).
import { domMax } from "motion/react";

export default domMax;
